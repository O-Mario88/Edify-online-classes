# ==============================================================================
# DEPRECATED — Legacy Finance Models
# ==============================================================================
# These models were the original simple finance implementation.
# They have been SUPERSEDED by the comprehensive ERP models in the
# `models/` package (student_profile, fees, invoicing, payments,
# accounting, audit, support).
#
# DO NOT USE these models. They remain only for migration compatibility.
# All new code should import from edify_backend.apps.finance.models (package).
# ==============================================================================

# The actual models are imported from the models/ package via __init__.py
# This file is intentionally left as a deprecation marker.
